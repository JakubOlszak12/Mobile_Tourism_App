/* eslint-disable require-jsdoc */
class Achievement {
  constructor(coordinates, name, text, type) {
    (this.coordinates = coordinates),
    (this.name = name),
    (this.text = text),
    (this.type = type);
  }
}
module.exports = Achievement;

